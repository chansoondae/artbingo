# 🐴 2026 병오년 새해 운세 룰렛

## Art Friends × 붉은 말의 해

---

## 📋 프로젝트 개요

### 컨셉
**"붉은 말의 기운을 담은 예술가와의 만남"**

2026년 병오년(丙午年)은 붉은색과 불의 기운을 지닌 '병(丙)'과 말을 상징하는 '오(午)'가 만나는 해. 
사용자가 룰렛을 돌려 자신의 2026년을 함께할 예술가를 만나고, 그 예술가의 작품 세계와 연결된 운세를 받는다.

### 타겟
- 아트프렌즈(Art Friends) 미술 전시 커뮤니티 회원
- 예술과 문화에 관심 있는 사용자

### 핵심 경험
1. **기대감** - 룰렛이 돌아가는 동안의 설렘
2. **몰입감** - 화려한 비주얼과 애니메이션
3. **특별함** - 나만의 예술가와 운세를 만나는 경험
4. **공유욕구** - SNS에 자랑하고 싶은 결과 화면

---

## 🎨 비주얼 디자인 컨셉

### 컬러 팔레트

```
Primary Colors (병오년 테마)
├── 주홍 (Vermillion)      #E63946  - 붉은 말의 붉은 기운
├── 진홍 (Crimson)         #9D0208  - 불의 열정
├── 금색 (Gold)            #FFD700  - 새해의 복과 번영
└── 검정 (Black)           #1A1A1A  - 동양화의 먹색

Accent Colors
├── 산호색 (Coral)         #FF6B6B  - 부드러운 강조
├── 호박색 (Amber)         #FFBE0B  - 따뜻한 빛
└── 아이보리 (Ivory)       #FFF8E7  - 한지 느낌의 배경

Gradient
└── 불꽃 그라디언트: #FFD700 → #E63946 → #9D0208
```

### 시각적 모티프

```
1. 말 (馬) 실루엣
   - 동양화 스타일의 질주하는 말
   - 불꽃/연기 효과와 결합
   
2. 불꽃 파티클
   - 룰렛 주변을 감싸는 불꽃
   - 결과 발표 시 폭발하는 스파크
   
3. 동양적 장식
   - 구름 문양 (雲紋)
   - 팔괘(八卦) 심볼
   - 전통 문양 테두리

4. 예술적 요소
   - 붓 터치 효과
   - 수묵화 번짐 효과
   - 캔버스/액자 프레임
```

---

## 🎰 룰렛 디자인 상세

### 룰렛 구조

```
┌─────────────────────────────────────────┐
│                                         │
│            ▼ (화살표/포인터)             │
│                                         │
│         ┌─────────────────┐            │
│        ╱                   ╲           │
│       │    중앙 허브         │          │
│       │   (말 실루엣 +       │          │
│       │    회전 이펙트)      │          │
│        ╲                   ╱           │
│         └─────────────────┘            │
│                                         │
│    ╭─────────────────────────╮         │
│   ╱  섹션1 │ 섹션2 │ 섹션3    ╲        │
│  │   ...   │  ...  │  ...     │        │
│  │   예술가 이름/이미지가       │        │
│  │   각 섹션에 배치            │        │
│   ╲  섹션N │ ...  │ ...      ╱        │
│    ╰─────────────────────────╯         │
│                                         │
│         외곽 장식 링                     │
│      (불꽃 + 전통문양)                   │
│                                         │
└─────────────────────────────────────────┘
```

### 룰렛 레이어 구성

```
Layer 5 (최상단): 화살표/포인터
Layer 4: 반짝이는 파티클 효과
Layer 3: 외곽 장식 링 (회전하지 않음)
Layer 2: 룰렛 휠 (회전하는 메인 영역)
Layer 1: 중앙 허브 (회전하지 않음, 고정)
Layer 0 (최하단): 배경 + 그림자
```

### 섹션 디자인

```
각 섹션 (12~16개 권장)
├── 배경: 그라디언트 또는 단색 (번갈아가며)
├── 예술가 대표 이미지 (작은 썸네일 또는 실루엣)
├── 예술가 이름 (곡선 텍스트로 휠에 맞게 배치)
└── 테두리: 금색 라인
```

---

## ✨ 애니메이션 상세 스펙

### 1. 시작 전 대기 상태

```javascript
// Idle Animation
{
  name: "idle",
  duration: "infinite",
  elements: {
    wheel: {
      animation: "gentle-float",      // 살짝 떠있는 듯한 움직임
      transform: "translateY(-2px) → translateY(2px)",
      timing: "ease-in-out",
      duration: "3s"
    },
    flames: {
      animation: "flicker",           // 불꽃 일렁임
      opacity: "0.6 → 1 → 0.6",
      scale: "0.95 → 1.05 → 0.95",
      duration: "1.5s"
    },
    particles: {
      animation: "float-up",          // 불씨가 위로 떠오름
      count: 20,
      spawn: "random around wheel edge",
      lifetime: "2-4s"
    },
    centerHub: {
      animation: "pulse-glow",        // 중앙 말 실루엣 빛남
      boxShadow: "0 0 20px → 0 0 40px gold",
      duration: "2s"
    }
  }
}
```

### 2. 룰렛 시작 (Spin Start)

```javascript
// 버튼 클릭 시 시작 애니메이션
{
  name: "spin-start",
  duration: "0.5s",
  sequence: [
    {
      time: 0,
      action: "button-press",
      animation: {
        scale: "1 → 0.95 → 1.1",
        ripple: "expand from center",
        sound: "click + whoosh"
      }
    },
    {
      time: 0.1,
      action: "wheel-charge",
      animation: {
        wheel: {
          transform: "rotate(-5deg)",   // 살짝 뒤로 감았다가
          filter: "brightness(1.2)"
        },
        flames: {
          scale: "1 → 1.5",
          intensity: "increase"
        }
      }
    },
    {
      time: 0.3,
      action: "wheel-release",
      animation: {
        wheel: {
          transform: "rotate(0deg) → start spinning",
          transition: "cubic-bezier(0.34, 1.56, 0.64, 1)"  // 탄성 있는 시작
        }
      }
    }
  ]
}
```

### 3. 메인 회전 (Main Spin)

```javascript
// 고속 회전 단계
{
  name: "main-spin",
  duration: "3-5s (dynamic)",
  phases: {
    // Phase 1: 가속 (0-1s)
    acceleration: {
      duration: "1s",
      rotation: "0 → 1800deg",        // 5바퀴
      easing: "ease-in",
      effects: {
        motionBlur: "0 → 10px",       // 속도감을 위한 블러
        flames: "intensify + trail",   // 불꽃이 꼬리처럼 따라옴
        sound: "spinning-whoosh (pitch increasing)"
      }
    },
    
    // Phase 2: 최고 속도 유지 (1-3s)
    peakSpeed: {
      duration: "2s",
      rotation: "constant 720deg/s",   // 초당 2바퀴
      easing: "linear",
      effects: {
        motionBlur: "10px (maintained)",
        wheel: "섹션이 거의 안 보일 정도로 빠름",
        flames: "maximum intensity",
        particles: "burst continuously",
        camera: "subtle shake (0.5px)",  // 미세한 화면 떨림
        sound: "sustained whoosh"
      }
    },
    
    // Phase 3: 감속 1차 (3-4s)
    deceleration1: {
      duration: "1s",
      rotation: "720deg/s → 180deg/s",
      easing: "ease-out",
      effects: {
        motionBlur: "10px → 3px",
        flames: "reduce intensity",
        sections: "start becoming visible",
        sound: "pitch decreasing"
      }
    },
    
    // Phase 4: 감속 2차 - 긴장감 (4-5s)
    deceleration2: {
      duration: "1-2s",
      rotation: "180deg/s → 5deg/s",
      easing: "cubic-bezier(0.25, 0.46, 0.45, 0.94)",
      effects: {
        motionBlur: "3px → 0",
        tick: "섹션 경계를 넘을 때마다 딸깍 소리",
        tickFrequency: "decreasing with speed",
        highlight: "현재 포인터 위치 섹션 강조",
        heartbeat: "subtle pulse effect on near-sections",
        sound: "tick-tick-tick (slowing)"
      }
    }
  }
}
```

### 4. 아슬아슬한 마무리 (Suspenseful Ending)

```javascript
// 가장 중요한 긴장감 연출
{
  name: "suspenseful-finish",
  duration: "2-4s",
  behavior: {
    // 거의 멈출 듯 말듯
    almostStop: {
      description: "섹션 경계에서 멈출 듯 하다가 조금 더 감",
      probability: "50%",
      animation: {
        rotation: "slow crawl",
        speed: "1-3deg/s",
        pause: "0.3-0.5s at boundary",
        resume: "tiny nudge forward"
      }
    },
    
    // 역회전 페이크 (선택적)
    reverseFeint: {
      probability: "20%",
      animation: {
        rotation: "forward → pause → back 2deg → forward 5deg",
        duration: "1s"
      }
    },
    
    // 딸깍거림
    tickEffect: {
      trigger: "crossing section boundary",
      animation: {
        wheel: "micro-bounce (1deg back and forth)",
        pointer: "vibrate",
        sound: "mechanical tick"
      }
    },
    
    // 최종 멈춤
    finalStop: {
      animation: {
        rotation: "settle with micro-oscillation",
        oscillation: "±0.5deg, 3 times, decreasing",
        duration: "0.8s"
      }
    }
  }
}
```

### 5. 결과 공개 (Result Reveal)

```javascript
// 당첨 결과 공개 시퀀스
{
  name: "result-reveal",
  duration: "3s",
  sequence: [
    // Step 1: 확정 순간 (0-0.5s)
    {
      time: 0,
      action: "lock-in",
      animation: {
        pointer: {
          transform: "scale(1) → scale(1.3) → scale(1)",
          filter: "drop-shadow(0 0 20px gold)"
        },
        winningSection: {
          highlight: "golden glow pulse",
          border: "2px → 4px gold"
        },
        sound: "triumphant chime"
      }
    },
    
    // Step 2: 불꽃놀이 폭발 (0.5-1.5s)
    {
      time: 0.5,
      action: "celebration-burst",
      animation: {
        fireworks: {
          origin: "winning section center",
          type: "radial explosion",
          colors: ["#FFD700", "#E63946", "#FF6B6B"],
          count: 50,
          spread: "full screen"
        },
        confetti: {
          count: 100,
          colors: "gold, red, coral",
          fall: "gentle cascade"
        },
        screen: {
          flash: "white, 0.1s, opacity 0.3"
        }
      }
    },
    
    // Step 3: 말 달리기 효과 (1-2s)
    {
      time: 1,
      action: "horse-run",
      animation: {
        horse: {
          type: "silhouette with fire trail",
          path: "gallop across screen",
          direction: "left → right",
          trail: "flame particles",
          duration: "1.5s"
        }
      }
    },
    
    // Step 4: 결과 카드 등장 (2-3s)
    {
      time: 2,
      action: "card-reveal",
      animation: {
        backdrop: {
          blur: "0 → 10px",
          darken: "overlay 0.7 opacity"
        },
        resultCard: {
          initial: "scale(0.3), opacity(0), rotateY(90deg)",
          final: "scale(1), opacity(1), rotateY(0)",
          easing: "spring(1, 100, 10, 0)",
          duration: "0.8s"
        }
      }
    }
  ]
}
```

### 6. 결과 카드 디자인

```
┌─────────────────────────────────────────────────────┐
│  ═══════════════════════════════════════════════   │
│         🐴 2026 병오년 당신의 예술가 🐴              │
│  ═══════════════════════════════════════════════   │
│                                                     │
│         ┌─────────────────────────┐                │
│         │                         │                │
│         │    [예술가 대표작       │                │
│         │     또는 초상]          │                │
│         │                         │                │
│         │    (불꽃 테두리 효과)    │                │
│         │                         │                │
│         └─────────────────────────┘                │
│                                                     │
│              ✦ 빈센트 반 고흐 ✦                    │
│              Vincent van Gogh                       │
│                                                     │
│  ─────────────────────────────────────────────     │
│                                                     │
│         " 2026년 당신의 운세 "                      │
│                                                     │
│    [예술가 스타일에 맞춘 운세 텍스트가              │
│     타이핑 효과로 나타남]                           │
│                                                     │
│  ─────────────────────────────────────────────     │
│                                                     │
│     [다시하기]              [결과 공유하기]         │
│                                                     │
│  ─────────────────────────────────────────────     │
│         Art Friends × 2026 병오년                  │
└─────────────────────────────────────────────────────┘
```

### 7. 결과 카드 애니메이션 상세

```javascript
{
  name: "result-card-animations",
  elements: {
    // 예술가 이미지
    artistImage: {
      entrance: {
        delay: "0.3s",
        animation: "fade-in + scale-up",
        duration: "0.6s"
      },
      idle: {
        animation: "subtle-float + golden-glow-pulse",
        duration: "4s infinite"
      }
    },
    
    // 예술가 이름
    artistName: {
      entrance: {
        delay: "0.6s",
        animation: "typewriter OR fade-up",
        letterSpacing: "tight → normal"
      },
      idle: {
        animation: "shimmer effect (gold gradient moving)",
        duration: "3s infinite"
      }
    },
    
    // 운세 텍스트
    fortuneText: {
      entrance: {
        delay: "1s",
        animation: "typewriter effect",
        speed: "50ms per character",
        cursor: "blinking brush cursor"
      },
      background: {
        animation: "ink-spread effect (watercolor style)"
      }
    },
    
    // 배경 장식
    decorations: {
      flames: {
        position: "corners",
        animation: "continuous flicker"
      },
      particles: {
        type: "floating embers",
        count: 20,
        animation: "rise and fade"
      },
      patterns: {
        type: "traditional cloud patterns",
        animation: "slow drift"
      }
    }
  }
}
```

---

## 🔊 사운드 디자인

### 사운드 이펙트 목록

```
sounds/
├── ui/
│   ├── button-hover.mp3      # 버튼 호버 시 부드러운 효과음
│   ├── button-click.mp3      # 버튼 클릭 시 확정감 있는 소리
│   └── card-flip.mp3         # 카드 뒤집기/등장 소리
│
├── spin/
│   ├── spin-start.mp3        # 룰렛 시작 - 바람/불꽃 소리
│   ├── spin-loop.mp3         # 고속 회전 중 - 윙윙거리는 소리
│   ├── spin-tick.mp3         # 섹션 지날 때마다 - 딸깍
│   ├── spin-slow.mp3         # 감속 시 - 점점 느려지는 톤
│   └── spin-stop.mp3         # 멈출 때 - 착지 느낌
│
├── celebration/
│   ├── reveal-fanfare.mp3    # 결과 공개 팡파레
│   ├── fireworks.mp3         # 불꽃놀이 효과음
│   ├── horse-gallop.mp3      # 말 달리는 소리
│   └── chime.mp3             # 신비로운 종소리
│
└── ambient/
    ├── fire-crackle.mp3      # 배경 불꽃 타닥거림
    └── wind-soft.mp3         # 부드러운 바람 소리
```

### 사운드 타이밍

```javascript
{
  soundTimeline: {
    // 대기 상태
    idle: {
      ambient: ["fire-crackle (loop, 30% volume)"]
    },
    
    // 회전 중
    spinning: {
      start: ["spin-start"],
      loop: ["spin-loop (pitch varies with speed)"],
      tick: ["spin-tick (on each section boundary)"],
      slowdown: ["spin-slow"]
    },
    
    // 결과 공개
    reveal: {
      stop: ["spin-stop"],
      fanfare: ["reveal-fanfare (delay 0.3s)"],
      celebration: ["fireworks", "horse-gallop"],
      card: ["card-flip", "chime"]
    }
  }
}
```

---

## 📱 반응형 디자인

### 브레이크포인트

```css
/* Mobile First */
@media (min-width: 320px) {
  /* 모바일 기본 */
  .roulette-wheel { width: 280px; height: 280px; }
  .result-card { width: 90vw; max-width: 350px; }
}

@media (min-width: 768px) {
  /* 태블릿 */
  .roulette-wheel { width: 400px; height: 400px; }
  .result-card { width: 450px; }
}

@media (min-width: 1024px) {
  /* 데스크톱 */
  .roulette-wheel { width: 500px; height: 500px; }
  .result-card { width: 550px; }
}
```

### 모바일 최적화

```
모바일 고려사항:
├── 터치 인터랙션
│   ├── 스와이프로 룰렛 돌리기 (선택적)
│   ├── 탭으로 버튼 누르기
│   └── 결과 카드 스와이프로 닫기
│
├── 성능 최적화
│   ├── 파티클 수 감소 (50% of desktop)
│   ├── 모션 블러 대신 opacity 변화
│   └── 복잡한 필터 효과 단순화
│
└── 레이아웃
    ├── 세로 중심 레이아웃
    ├── 버튼 크기 44px 이상 (터치 영역)
    └── 여백 조정
```

---

## 🛠 기술 스택 및 구현

### 기술 스택

```
Framework: Next.js 14+ (App Router)
Styling: Tailwind CSS + CSS Modules
Animation: 
  ├── Framer Motion (메인 애니메이션)
  ├── CSS Animations (간단한 효과)
  └── Canvas API (파티클 시스템)
Audio: Howler.js 또는 Web Audio API
State: React useState/useReducer
```

### 컴포넌트 구조

```
src/
├── app/
│   └── fortune-2026/
│       └── page.tsx                 # 메인 페이지
│
├── components/
│   └── fortune-roulette/
│       ├── FortuneRoulette.tsx      # 메인 컨테이너
│       ├── RouletteWheel.tsx        # 룰렛 휠
│       ├── RoulettePointer.tsx      # 화살표/포인터
│       ├── SpinButton.tsx           # 돌리기 버튼
│       ├── ParticleSystem.tsx       # 불꽃 파티클
│       ├── ResultCard.tsx           # 결과 카드
│       ├── HorseAnimation.tsx       # 말 달리기 효과
│       ├── Fireworks.tsx            # 불꽃놀이 효과
│       └── SoundManager.tsx         # 사운드 컨트롤
│
├── hooks/
│   ├── useRouletteLogic.ts          # 룰렛 로직 (결과 결정 등)
│   ├── useSpinAnimation.ts          # 회전 애니메이션 제어
│   └── useSoundEffects.ts           # 사운드 효과 훅
│
├── data/
│   ├── artists.ts                   # 예술가 데이터
│   └── fortunes.ts                  # 운세 데이터
│
├── lib/
│   ├── particleEngine.ts            # 파티클 시스템 엔진
│   └── easing.ts                    # 커스텀 이징 함수
│
└── styles/
    └── fortune-roulette.css         # 전용 스타일
```

### 주요 상태 관리

```typescript
interface RouletteState {
  phase: 'idle' | 'spinning' | 'slowing' | 'stopping' | 'result';
  selectedIndex: number | null;
  rotationDegree: number;
  spinSpeed: number;
  soundEnabled: boolean;
}

type RouletteAction =
  | { type: 'START_SPIN' }
  | { type: 'UPDATE_ROTATION'; degree: number; speed: number }
  | { type: 'ENTER_SLOWDOWN' }
  | { type: 'STOP'; selectedIndex: number }
  | { type: 'SHOW_RESULT' }
  | { type: 'RESET' }
  | { type: 'TOGGLE_SOUND' };
```

---

## 🎯 사용자 플로우

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  1. 랜딩                                                    │
│     ├── 병오년 테마 인트로 화면                              │
│     ├── "나의 2026년 예술가 찾기" 헤더                       │
│     └── 아이들 애니메이션 중인 룰렛                          │
│                                                             │
│                         ↓                                   │
│                                                             │
│  2. 룰렛 대기                                               │
│     ├── 룰렛 휠 + 불꽃 효과                                 │
│     ├── [룰렛 돌리기] 버튼                                  │
│     └── (선택) 사운드 ON/OFF 토글                           │
│                                                             │
│                         ↓                                   │
│                                                             │
│  3. 회전 중                                                 │
│     ├── 고속 회전 (3-4초)                                   │
│     ├── 점점 감속                                           │
│     └── 아슬아슬한 마무리 (1-2초)                           │
│                                                             │
│                         ↓                                   │
│                                                             │
│  4. 결과 공개                                               │
│     ├── 불꽃놀이 + 말 달리기 효과                           │
│     ├── 결과 카드 등장                                      │
│     ├── 예술가 정보 + 운세 표시                             │
│     └── [다시하기] / [공유하기] 버튼                         │
│                                                             │
│                         ↓                                   │
│                                                             │
│  5. 공유 (선택)                                             │
│     ├── 결과 이미지 생성                                    │
│     └── SNS 공유 또는 이미지 저장                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎲 룰렛 로직

### 결과 결정 알고리즘

```typescript
interface SpinConfig {
  minRotations: number;      // 최소 회전 수 (5바퀴)
  maxRotations: number;      // 최대 회전 수 (8바퀴)
  baseDuration: number;      // 기본 회전 시간 (4초)
  suspenseDuration: number;  // 긴장감 연출 시간 (2초)
}

function determineResult(artists: Artist[], config: SpinConfig) {
  // 1. 랜덤하게 결과 예술가 선택
  const selectedIndex = Math.floor(Math.random() * artists.length);
  
  // 2. 해당 인덱스에 도달하기 위한 총 회전각 계산
  const sectionAngle = 360 / artists.length;
  const targetAngle = selectedIndex * sectionAngle;
  
  // 3. 랜덤 회전 수 + 목표 각도
  const rotations = config.minRotations + 
    Math.random() * (config.maxRotations - config.minRotations);
  const totalRotation = (rotations * 360) + targetAngle;
  
  return {
    selectedIndex,
    selectedArtist: artists[selectedIndex],
    totalRotation,
    duration: config.baseDuration + config.suspenseDuration
  };
}
```

### 이징 함수

```typescript
// 커스텀 이징: 빠르게 시작 → 천천히 감속 → 아슬아슬한 끝
function customSpinEasing(t: number): number {
  // t: 0 ~ 1 (진행률)
  
  if (t < 0.1) {
    // 0-10%: 급가속
    return easeInQuad(t / 0.1) * 0.3;
  } else if (t < 0.5) {
    // 10-50%: 최고 속도 유지
    return 0.3 + (t - 0.1) / 0.4 * 0.4;
  } else if (t < 0.8) {
    // 50-80%: 1차 감속
    return 0.7 + easeOutQuad((t - 0.5) / 0.3) * 0.2;
  } else {
    // 80-100%: 아슬아슬한 감속 (매우 천천히)
    return 0.9 + easeOutExpo((t - 0.8) / 0.2) * 0.1;
  }
}
```

---

## 📊 예술가 표시 방식

### 룰렛 섹션 구성

151명의 예술가 전체를 룰렛에 표시하면 너무 복잡해지므로:

```
옵션 1: 랜덤 12~16명 선택
├── 매번 새로고침/재시작 시 랜덤하게 선택
├── 다양한 시대/스타일 균형 있게 배치
└── 실제 결과는 151명 전체에서 랜덤 선택

옵션 2: 카테고리별 대표작가
├── 르네상스: 다빈치, 미켈란젤로
├── 인상파: 모네, 반 고흐
├── 현대미술: 피카소, 달리
└── 한국 작가: 김환기, 백남준

옵션 3: 인지도 상위 12~16명 고정
└── 인지도 점수 높은 순으로 선택

권장: 옵션 1 (다양성과 재미 보장)
```

### 섹션 내 표시 정보

```
각 섹션에 표시할 내용:
├── 예술가 이름 (한글, 곡선 텍스트)
├── 작은 아이콘/심볼 (예술가 대표 이미지 또는 대표작 썸네일)
└── 섹션 배경색 (예술가 대표 색상 or 번갈아가며)
```

---

## 🎁 추가 기능 (선택적)

### 1. 공유 기능

```typescript
interface ShareOptions {
  platforms: ['kakao', 'twitter', 'instagram', 'facebook', 'clipboard'];
  
  generateShareImage: () => Promise<Blob>;  // 결과 카드를 이미지로
  generateShareText: () => string;          // 공유용 텍스트
}

// 공유 텍스트 예시
"🐴 2026 병오년 나의 예술가: 빈센트 반 고흐
✨ 별이 빛나는 밤처럼, 올해 당신의 창의력이 폭발합니다!
#아트프렌즈 #2026운세 #병오년"
```

### 2. 결과 저장/히스토리

```typescript
interface FortuneHistory {
  date: Date;
  artist: Artist;
  fortune: string;
}

// localStorage에 저장하여 이전 결과 확인 가능
```

### 3. 애니메이션 프리셋

```typescript
// 사용자가 선택 가능한 애니메이션 스타일
type AnimationPreset = 'dynamic' | 'elegant' | 'minimal';

// dynamic: 모든 효과 활성화 (기본)
// elegant: 파티클 줄이고 부드러운 애니메이션
// minimal: 핵심 애니메이션만 (저사양 기기용)
```

---

## ⚡ 성능 고려사항

### 최적화 전략

```
1. 애니메이션 성능
├── transform, opacity만 사용 (GPU 가속)
├── will-change 적절히 사용
├── requestAnimationFrame 활용
└── 복잡한 애니메이션은 Canvas로 분리

2. 파티클 시스템
├── Object pooling으로 GC 최소화
├── 화면 밖 파티클 즉시 제거
├── 저사양 기기 자동 감지 → 파티클 수 감소

3. 이미지 최적화
├── Next.js Image 컴포넌트 활용
├── WebP 포맷 우선
├── 적절한 사이즈 + lazy loading

4. 사운드 최적화
├── 미리 로드 (preload)
├── 압축된 포맷 사용
└── 사운드 풀링
```

### 접근성

```
1. 모션 감소 모드 지원
├── prefers-reduced-motion 미디어 쿼리
└── 핵심 인터랙션만 유지, 화려한 효과 제거

2. 키보드 접근성
├── 버튼 포커스 상태 명확히
├── Enter/Space로 룰렛 돌리기
└── Escape로 결과 닫기

3. 스크린 리더
├── 적절한 aria-label
├── 결과 발표 시 aria-live
└── 로딩/회전 상태 안내
```

---

## 📅 구현 우선순위

### Phase 1: 핵심 기능 (MVP)
```
□ 기본 룰렛 UI
□ 회전 애니메이션 (시작/멈춤)
□ 랜덤 결과 선택 로직
□ 기본 결과 카드
```

### Phase 2: 애니메이션 강화
```
□ 불꽃 파티클 시스템
□ 아슬아슬한 감속 효과
□ 불꽃놀이 효과
□ 말 달리기 애니메이션
```

### Phase 3: 완성도
```
□ 사운드 효과
□ 반응형 최적화
□ 공유 기능
□ 성능 최적화
```

---

## 🎨 참고 비주얼 레퍼런스

### 스타일 참고
- 전통 동양화의 먹색과 주홍색 대비
- 불꽃놀이/축제 느낌의 화려함
- 고급스러운 금색 악센트
- 카지노 룰렛의 긴장감 있는 연출

### 애니메이션 참고
- 슬롯머신의 릴 회전 및 정지 느낌
- 가챠/뽑기 게임의 결과 공개 연출
- 불꽃놀이 파티클 효과
- 동양화 붓터치 느낌의 트랜지션

---

*Last Updated: 2025-12-31*
*Version: 1.0*
*Project: Art Friends × 2026 병오년 운세 룰렛*
